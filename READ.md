Before all these script are run make sure to install the dependency
'pip install gmpy2'

For Challenge 2 and 3, The point is to show how specific weaknesses in key generation allow RSA keys to be broken. thus just computing the factors unlike challenge 1 where we are given a ciphertext to decrypt.
