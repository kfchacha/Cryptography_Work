For both codes of LAB 2. Using cbc and ctr mode of AES for decryption, make sure to install pycryptodome library. Use the command "pip install pycryptodome
" which will setup the environment prior to running it.
